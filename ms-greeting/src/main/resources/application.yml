server:
  port: 19090
logging:
  level:
    root: trace
    org.apache.tomcat.util.net: info
spring:
  application:
    name: SERVICE-GREETING
eureka:
  client:
    service-url:
      defaultZone: http://localhost:18761/eureka
